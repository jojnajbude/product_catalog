export enum SortBy {
  Newest = 'Newest',
  Oldest = 'Oldest',
  Cheaper = 'Cheaper',
  More_Expensive = 'Move expensive',
}

export const sortByOptions = [
  SortBy.Newest,
  SortBy.Oldest,
  SortBy.Cheaper,
  SortBy.More_Expensive
];